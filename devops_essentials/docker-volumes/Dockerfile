# Use the Alpine base image
FROM alpine:latest

# Install curl for demonstration purposes
RUN apk add --no-cache curl

# Create a volume at /data
VOLUME /data

# Start a shell to keep the container running for demonstration
CMD ["/bin/sh"]
